module.exports = {
    content: ["./src/**/*.{html,js}"],
    darkMode: 'class',
    theme: {
        extend: {},
        container: {
            center: true,
        }
    },
    variants: {
        extend: {
            opacity: ['disabled'],
        },
    },
    plugins: [],
}