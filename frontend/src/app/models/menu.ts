export class Menu {
    constructor(
        public mid: number,
        public vid: number,
        public name: string,
        public quantity: number,
        public price: number,
        public active: boolean,
        public del: boolean
    ) { }
}
